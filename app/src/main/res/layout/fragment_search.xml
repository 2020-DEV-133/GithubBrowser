<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
       <variable
           name="homeViewModel"
           type="com.debin.githubbrowser.HomeViewModel" />
    </data>

<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.SearchFragment">

    <EditText
        android:id="@+id/edt_search"
        android:layout_width="0dp"
        android:layout_height="@dimen/dimen50"
        android:layout_margin="@dimen/dimen4"
        android:paddingStart="@dimen/dimen16"
        android:paddingEnd="@dimen/dimen16"
        android:backgroundTint="@color/colorWhite"
        android:inputType="text"
        android:autofillHints="@string/enter_github_user_name"
        android:hint="@string/enter_github_user_name"
        android:background="@android:drawable/edit_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/icon_search"
        />

    <ImageButton
        android:id="@+id/icon_search"
        android:layout_width="@dimen/dimen50"
        android:layout_height="@dimen/dimen50"
        android:padding="@dimen/dimen4"
        android:layout_margin="@dimen/dimen4"
        android:background="@color/colorAccent"
        android:src="@drawable/ic_search"
        android:onClick="@{() -> homeViewModel.searchUser()}"
        android:contentDescription="@string/search_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@+id/edt_search"/>

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="@{homeViewModel.progress}"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

     <Button
         android:id="@+id/button_add_user"
         android:layout_width="100dp"
         android:layout_height="wrap_content"
         android:text="@string/add_user"
         android:layout_marginBottom="@dimen/dimen40"
         android:textAllCaps="false"
         android:visibility="gone"
         android:textColor="@color/colorWhite"
         android:background="@color/colorPrimaryDark"
         app:layout_constraintStart_toStartOf="parent"
         app:layout_constraintEnd_toEndOf="parent"
         app:layout_constraintBottom_toBottomOf="parent"/>


</androidx.constraintlayout.widget.ConstraintLayout>
</layout>